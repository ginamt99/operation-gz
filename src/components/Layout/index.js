import react from 'react';
import styled from 'styled-components';

const LayoutComp = ({ className }) => {

    return (
        <>
        </>
    );
};

const Layout = styled(LayoutComp)`



`

export default Layout;